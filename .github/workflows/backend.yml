name: Deploy Backend

on:
  push:
    branches: [ main ]
    paths:
      - 'backend/**'
      - 'appspec.yaml'
      - '.github/workflows/backend.yml'
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

env:
  AWS_REGION: ap-northeast-2
  ECR_REPOSITORY: focus-tracking-platform-repo
  ECS_CLUSTER: focus-tracking-platform-dev-cluster
  ECS_SERVICE: focus-tracking-platform-dev-svc
  ECS_TASK_FAMILY: focus-tracking-platform-dev-task
  CODEDEPLOY_APP: focus-tracking-platform-dev-cdapp
  CODEDEPLOY_GROUP: focus-tracking-platform-dev-dg
  CONTAINER_NAME: app

jobs:
  deploy:
    name: Build → Push → Deploy
    runs-on: ubuntu-24.04-arm   # ★ 네이티브 ARM64 러너

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      # ❌ QEMU 제거 (네이티브 ARM이라 불필요)
      # ❌ Buildx도 제거 가능 (기본 docker build로 충분)

      - name: Log in to Amazon ECR
        id: ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Docker 빌드 + ECR 푸시 (네이티브 ARM64)
        id: build
        env:
          ECR_REGISTRY: ${{ steps.ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          set -e
          IMAGE=$ECR_REGISTRY/$ECR_REPOSITORY
          docker build \
            -t $IMAGE:$IMAGE_TAG \
            -t $IMAGE:latest \
            ./backend
          docker push $IMAGE:$IMAGE_TAG
          docker push $IMAGE:latest
          echo "image=$IMAGE:$IMAGE_TAG" >> $GITHUB_OUTPUT

      - name: Download current task definition
        run: |
          aws ecs describe-task-definition \
            --task-definition ${{ env.ECS_TASK_FAMILY }} \
            --query taskDefinition \
            > taskdef.json

      - name: 새 Task Definition 렌더링 (이미지 + 환경변수)
        id: render
        uses: aws-actions/amazon-ecs-render-task-definition@v1
        with:
          task-definition: taskdef.json
          container-name: ${{ env.CONTAINER_NAME }}
          image: ${{ steps.build.outputs.image }}
          environment-variables: |
            NODE_ENV=production
            GOOGLE_CLIENT_ID=${{ secrets.GOOGLE_CLIENT_ID }}
            GOOGLE_CLIENT_SECRET=${{ secrets.GOOGLE_CLIENT_SECRET }}
            AUTH_SECRET=${{ secrets.AUTH_SECRET }}
            GOOGLE_REDIRECT_URI=${{ secrets.GOOGLE_REDIRECT_URI }}
            APP_URL=${{ secrets.APP_URL }}

      - name: Deploy to ECS (CodeDeploy Blue/Green)
        uses: aws-actions/amazon-ecs-deploy-task-definition@v2
        with:
          task-definition: ${{ steps.render.outputs.task-definition }}
          service: ${{ env.ECS_SERVICE }}
          cluster: ${{ env.ECS_CLUSTER }}
          codedeploy-appspec: appspec.yaml
          codedeploy-application: ${{ env.CODEDEPLOY_APP }}
          codedeploy-deployment-group: ${{ env.CODEDEPLOY_GROUP }}
          wait-for-service-stability: true